PORT=3000
NODE_ENV=development

# Session Secret - Change this to a random string
SESSION_SECRET=
MAIL_ENCRYPTION_KEY=

# Base URL (for email links)
BASE_URL=http://localhost:3000

# SMTP Configuration (optional - configure via admin panel)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# FROM_EMAIL=
# FROM_NAME=